---
skill: science-training-system
category: planning
description: 已有表要改。每次 1–2 個變數，不重排整週。
---

# 改計畫

觸發：容量太大、想換動作、恢復不過來。重排整週 → `design-plan.md`。下一組 → `session-adjust.md`。

1. 先對齊現用表。對方說用存檔才讀 `user/plan.md`；否則請對方貼。
2. 每次只改 **1–2** 個變數。優先砍輔助，不砍主項品質。
3. L1：`guardrails/principles.yaml`。MEV/MRV 是 L2 啟發式，不當擋門。
4. 改重量：`core/load.py`。類型 B 禁止手算掛重百分比。
5. 疼痛：適宜劑量（`barbell_medicine`）。尖銳痛該動作停，不換成痛的變式硬做。
6. 複述改了什麼，確認後才出新表。末附 Volume（先 header，再 MEV／MAV／MRV）。有 🔴 優先再砍輔助。
