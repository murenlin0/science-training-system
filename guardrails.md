---
skill: science-training-system
category: guardrails
description: L0/L1 必須遵守；L2 數字與輔助菜單可討論、塊中途不改。
---

# 約束

- L0 `core/identities.yaml`
- L1 `guardrails/principles.yaml`
- L2 `config/parameters.yaml` + `config/assistance.yaml`
- 不是原則：`evidence/rejected.yaml`

## 設計時強制

0. 先問主項。
1. 對齊目標再套原則。
2. 類型 B 百分比打在 L。
3. 主項不力竭；主項動作不得再列輔助。
4. GPP 不反向改主項。有氧 %HRR。不用 ACWR。回訓不用舊 1RM。

## 輔助

選完主項只讀 `config/assistance.yaml` 對應 key。西岸「必須用特殊動作」不是原則。
